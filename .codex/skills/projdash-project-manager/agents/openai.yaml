interface:
  display_name: "ProjDash Project Manager"
  short_description: "Reconcile notes into calibrated ProjDash plans."
  default_prompt: "Use $projdash-project-manager to reconcile unreconciled notes into the project plan, infer estimates, check slippage, and summarize current priorities."

policy:
  allow_implicit_invocation: true
